# 自定义字体

常见关于 font 的 CSS 属性有：

* `font-family`：
* `font-style`：
* `font-variant`：
* `font-weight`：
* `font-size`：
* `line-height`：

如何使用@font-face规则来加载自定义字体

确保自定义字体在各种浏览器中都能正常工作，需要提供不同格式的字体文件，具体过程

不同 font-weight 下的字体支持

font 字体库文件的大小优化？如何瘦身？
目前引用的霞鹜文楷字体库大小为 17 MB，属实有点大