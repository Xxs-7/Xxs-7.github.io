# react 源码解读

源码架构分层
代码执行流程
初次渲染核心函数是哪些？处理过程？
更新渲染核心函数是哪些？处理过程？
rootFiber 和 fiberRoot
workInProgress 树的构建
优先级的实现方案，expiration time 和 lane 模型
任务调度的实现？如何实现可中断渲染

React 18 的 Concurrent 模式是基于 legacy 模式而来，在主流程逻辑的基础上增加的新特性的处理。所以第一步应该是关注主逻辑流程而不是新特性，此时阅读 legacy 模式的处理函数来理解主流程更有效率。进阶再阅读 concurrent 模式的处理函数。

createRoot 初始化 rootFiber 和 fiberRoot 的相关逻辑。

